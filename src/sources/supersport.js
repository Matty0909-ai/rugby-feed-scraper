// src/sources/supersport.js

/**
 * Placeholder SuperSport scraper.
 * For now, this returns empty arrays so the pipeline works.
 * You can fill this in later once we've inspected the SuperSport HTML.
 */

export async function fetchSuperSportData() {
  return {
    fixtures: [],
    results: []
  };
}
